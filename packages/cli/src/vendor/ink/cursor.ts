/**
 * STUB — `cursor.ts` was referenced by frame.ts but not present in the leaked
 * Claude Code source. Only the `Cursor` shape is used (frame state + cursor
 * ops), reconstructed here from frame.ts's usage.
 */
export type Cursor = {
  x: number;
  y: number;
  visible: boolean;
};
