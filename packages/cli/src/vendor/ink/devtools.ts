/**
 * STUB — `devtools.ts` was a dev-only optional module (react-devtools-core
 * connection) referenced by reconciler.ts via a guarded dynamic import inside
 * a try/catch. It was not present in the leaked source. Lucky does not ship
 * React DevTools wiring, so this is intentionally empty.
 */
export {};
